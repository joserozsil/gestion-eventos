'use strict'
import Model from '../models'
function saveHistory ( descripcion ){
	return function (req, res, next) {
        try {
			
		} catch( e ) {
			next(e)
		}
    }

}

module.exports = saveHistory 
